[project]
name = "foundation"
version = "0.1.0"
description = "Personal Finance Monte Carlo Simulator"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask==3.1.0",
    "flask-cors==5.0.0",
    "pydantic==2.10.0",
    "numpy==1.26.4",
]

[project.optional-dependencies]
dev = [
    "requests==2.32.5",
]
