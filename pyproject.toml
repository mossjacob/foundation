[project]
name = "foundation"
version = "0.1.0"
description = "Personal Finance Monte Carlo Simulator"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fastapi==0.115.0",
    "pydantic==2.10.0",
    "numpy==1.26.4",
]

[project.optional-dependencies]
dev = [
    "uvicorn==0.34.0",
    "requests==2.32.5",
]
